// Sol Router facade
//
// The router package is intentionally split by responsibility:
// - router_hmr.mbt: HMR helpers
// - router_config.mbt: config/chunk manifest
// - route_params.mbt: request param extraction and PageProps
// - route_registration.mbt: Mars route registration
// - route_rendering.mbt: HTML rendering helpers
