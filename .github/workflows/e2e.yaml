name: e2e

on:
  workflow_dispatch:
  push:
    branches:
      - main
  pull_request:

jobs:
  # E2E tests with Playwright
  e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - uses: pnpm/action-setup@v4
        with:
          run_install: false
      - run: pnpm install
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: "pnpm"
      - uses: illusory0x0/setup-moonbit@v0.1.0
        with:
          version: stable
      - run: |
          moon update
          moon install
      - name: Build MoonBit
        run: moon build --target js
      - name: Install Playwright Chromium
        run: npx playwright install chromium --with-deps
      - name: E2E tests (core)
        run: pnpm test:e2e
      # sol_app e2e tests disabled - example has build errors (unbound identifiers)
      # - name: E2E tests (sol_app)
      #   working-directory: examples/sol_app
      #   run: |
      #     pnpm install
      #     pnpm test:e2e
