{
  "name": "sol-tsx-example",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build:client": "node build-client.mjs",
    "dev": "npm run build:client && npx tsx server.ts",
    "build": "npm run build:client",
    "serve": "npx tsx server.ts"
  },
  "dependencies": {
    "@hono/node-server": "^1.0.0",
    "@luna_ui/luna": "workspace:*",
    "@sol/core": "workspace:*",
    "hono": "^4.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.21.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
